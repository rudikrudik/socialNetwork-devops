---
- name: Preconfig all servers 
  hosts: all
  roles:
    - role: preconfig
      tags: preconfig
    - role: build
      tags: build
    - role: deploy
      tags: deploy
    - role: zabbix-agent
      tags: zabbix-agent

# add to initdb script
#CREATE USER zbx_monitor WITH PASSWORD '<PASSWORD>' INHERIT;
#GRANT pg_monitor TO zbx_monitor;